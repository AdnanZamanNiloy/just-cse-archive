# Inventory Management Bakery

- Type: independent academic project
- Contents: a deterministic Python bakery inventory and sales simulation, with CSV history and inventory-trend plots in `output/`.
- Technologies: Python, pandas, NumPy, and Matplotlib

Run the simulation from this directory with:

```text
python main.py --days 7
```

Install the dependencies listed in `requirements.txt` first.
